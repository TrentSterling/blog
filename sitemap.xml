---
---
<?xml version="1.0" encoding="UTF-8"?>
<!--
  Posts and real pages only, on purpose.

  Generated tag, category and pagination pages carry noindex,follow (see
  _includes/head.html), so they are deliberately left out of this file.
  A sitemap is a discovery signal, not a guarantee of indexing.
  jekyll-sitemap skips generating its own when this file exists.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>{{ '/' | absolute_url }}</loc>
  </url>
  {%- for post in site.posts %}
  {%- unless post.sitemap == false %}
  <url>
    <loc>{{ post.url | absolute_url }}</loc>
    <lastmod>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</lastmod>
  </url>
  {%- endunless %}
  {%- endfor %}
  {%- for tab in site.tabs %}
  {%- unless tab.sitemap == false %}
  <url>
    <loc>{{ tab.url | absolute_url }}</loc>
  </url>
  {%- endunless %}
  {%- endfor %}
</urlset>
